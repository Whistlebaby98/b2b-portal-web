# 企采云对公商城领域词汇

本词汇表是对公商城的通用语言。词条描述业务对象、角色、关系和业务不变量；界面、接口、数据库及其他技术实现不属于本文件的定义范围。

## 参与者与组织

### CustomerOrganization（客户组织）

使用商城进行企业采购的法人或经营主体。客户组织拥有独立的资质、采购规则、价格权益、结算资料和订单归属。一个客户组织可以有多个 BuyerUser。

### BuyerUser（买方用户）

代表客户组织进行浏览、选品、建购物车和提交采购订单的自然人。买方用户通过 OrganizationMembership 关联到一个或多个 CustomerOrganization；其看到的价格、库存、可用结算方式和订单数据取决于当前组织及其授权范围。

### OrganizationMembership（组织成员关系）

描述 BuyerUser 与 CustomerOrganization 的关系，包括成员状态、职责和可执行的业务动作。成员关系不改变订单的归属：订单始终归属于提交时的 CustomerOrganization。

### ActiveOrganizationContext（当前组织上下文）

买方用户当前正在代表的客户组织。它决定本次会话中的目录、价格、促销资格、购物车和订单视图。切换当前组织后，不应把一个组织的购物车、价格或订单混入另一个组织。

### BuyerRole（买方职责）

买方职责的业务分类，例如采购员、采购负责人或财务联系人。职责用于说明谁可以提交订单、查看结算资料或接收通知；职责本身不等同于审批权限。

### Approver（审批人）

负责依据客户组织的采购政策审核订单的人员或组织节点。审批人可以属于客户组织，也可以由客户组织指定的外部流程承接。审批职责与买方下单职责分离。

## 商品、价格与促销

### Product（商品）

可被客户了解和采购的商品族。商品包含名称、描述、分类、品牌、规格属性、合规信息和可售状态；具体可订购的规格由 SKU 表示。

### SKU（库存单位）

商品的可交易规格和库存单位，具有唯一的业务编码。SKU 定义规格、计量单位、包装约束、交付承诺和可售库存；OrderLine 必须引用一个确定的 SKU，而不能只引用商品族。

### CatalogCategory（商品分类）

用于组织和筛选 Product/SKU 的业务分类。分类可以形成层级，但分类归属不改变商品的价格或订单归属。

### InventoryAvailability（可售库存）

某个 SKU 在当前组织和交付条件下可被承诺的数量。可售库存可能是现货、预留量或可采购量；展示的数量不代表订单已经锁定库存。

### DeliveryPromise（交付承诺）

针对 SKU、数量、收货地和当前时间给出的预计发货或到货范围。交付承诺是下单时的业务快照，后续变化应通过订单履约信息表达。

### PriceOffer（价格报价）

针对 CustomerOrganization、SKU、数量区间和有效期给出的可交易单价及适用条件。PriceOffer 可以是组织协议价、阶梯价或标准价；同一订单行只能采用一个经过规则匹配的报价。

### Campaign（促销活动）

在时间、商品、组织、数量或金额等条件满足时提供优惠或权益的业务规则。Campaign 可以影响 PriceOffer、赠品、运费或其他订单权益；促销资格必须在提交订单时重新核验。

### ContractEntitlement（协议权益）

客户组织因合同或等级获得的价格、额度、交付或服务权益。协议权益优先于通用公开权益，但不能突破 SKU 的可售状态或客户组织的合规限制。

## 采购与结算

### Cart（购物车）

客户组织在采购过程中暂存的 SKU、数量、价格展示和交付偏好集合。购物车属于 ActiveOrganizationContext；它是可编辑草稿，不是订单，也不锁定库存或价格。

### CartLine（购物车行）

购物车中的一个 SKU 及其采购数量、计量单位和当前价格提示。数量必须为正整数（或 SKU 允许的最小增量），删除最后一行后购物车为空。

### CheckoutSession（结算会话）

把 Cart 转换为订单前，对收货、开票、结算、促销和价格进行确认的短生命周期业务会话。结算会话记录确认时的校验结果和金额预览；它完成或失效后不能重复提交同一结果。

### Order（采购订单）

客户组织正式提交的采购请求，包含订单行、金额、价格/促销快照、收货与开票信息、结算条款及审批状态。订单一经提交即获得唯一编号，并与提交时的 CustomerOrganization 和 BuyerUser 建立不可变归属。

### OrderLine（订单行）

订单中的 SKU、数量、成交单价、促销分摊、税务信息和交付承诺快照。订单行的金额由提交时的快照决定，不随目录价格后续变化而回算。

### ApprovalRequest（审批请求）

由 Order 提交触发的审批工作项，记录审批链、当前节点、处理人、意见和时间线。MVP 中每一笔订单都必须先生成状态为“待审批”的 ApprovalRequest；订单未通过审批前不得进入已确认采购状态。

### ApprovalPolicy（审批政策）

客户组织决定审批链、节点顺序、金额阈值及例外处理的业务规则。MVP 采用统一入口：订单无论金额和来源都进入待审批；后续可按组织政策细分。

### OrderStatus（订单状态）

订单在采购生命周期中的业务状态，例如待审批、审批中、已通过、已驳回、已取消、履约中、已完成。状态只能沿允许的业务转移变化，并应保留可追溯时间线。

### ApprovalStatus（审批状态）

ApprovalRequest 的状态，例如待审批、审批中、已通过、已驳回、已撤回或已失效。审批状态与履约状态分开表达，避免把“等待审批”误认为“缺货”或“已付款”。

### ShippingAddress（收货地址）

订单的货物接收地点及联系人信息，归属于 CustomerOrganization。提交时形成地址快照；后续地址簿变化不应修改历史订单。

### InvoiceProfile（开票资料）

客户组织用于开具发票的抬头、税号、地址电话、开户行及账号、发票类型和收票信息。提交订单时选定的开票资料形成订单快照，并受组织资质校验约束。

### PaymentTerm（结算条款）

约定客户组织如何结算订单，例如账期、授信额度、预付款或线下对公支付。可用条款由组织资质和订单金额决定；选择条款不等同于支付已完成。

### CreditLimit（授信额度）

客户组织在 PaymentTerm 下可使用的信用额度及已用额度。额度校验应在结算确认和订单提交时执行；订单处于待审批并不意味着额度已经最终扣减。

### Tax（税务信息）

订单及订单行使用的税率、税额和含税/未税口径。税务信息应与 InvoiceProfile 和 SKU 的税务属性一致，并在订单提交时固化。

### OrderTotal（订单总额）

由订单行金额、促销优惠、运费、税额和其他可解释费用组成的金额快照。买方应能区分商品小计、优惠、税额和应付/应结算金额。

## 业务关系与不变量

- 一个 CustomerOrganization 可以拥有多个 BuyerUser、ShippingAddress、InvoiceProfile 和 PaymentTerm；一个 BuyerUser 可以通过不同的 OrganizationMembership 代表多个组织。
- ActiveOrganizationContext 必须始终指向一个有效的 CustomerOrganization。Cart、CheckoutSession 和新建 Order 不能跨组织共享。
- Product 是展示与归类单位，SKU 是交易与库存单位；没有有效 SKU 的商品不能进入 Cart 或 Order。
- PriceOffer 与 Campaign 的适用资格以客户组织、SKU、数量和有效期为条件，且必须在 CheckoutSession 和 Order 提交时再次确认。
- Cart 不是订单；加入购物车、价格预览或创建 CheckoutSession 都不会产生采购承诺。
- 每个提交成功的 Order 都有且只有一个当前 ApprovalRequest。MVP 下其初始 ApprovalStatus 为“待审批”，买方侧不承担审批动作。
- Order、OrderLine、ShippingAddress、InvoiceProfile、PaymentTerm 和金额均保留提交时快照，以便审计和对账。
